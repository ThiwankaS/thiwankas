flyctl machines list --app <APP_NAME>

flyctl machines destroy <MACHINE_ID> --app <APP_NAME>

flyctl volumes list --app <APP_NAME>

flyctl volumes destroy <VOLUME_ID> -a <APP_NAME> -y

flyctl apps destroy <APP_NAME> --yes

flyctl apps list
